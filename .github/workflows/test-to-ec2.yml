name: Deploy to Test Git Server

on:
  push:
    branches:
      - master  # Ветка для тестирования

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3


      - name: Deploy on Test Git Server
        run: |
            cd /home/ && docker-compose pull && docker-compose up -d"
